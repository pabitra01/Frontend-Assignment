import React from "react";

const EmptyContent = () => {
  return (
    <div className="w-full h-full flex justify-center items-center body-1">
      Enter the json data to render form
    </div>
  );
};

export default EmptyContent;
